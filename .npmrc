; Quieter, faster installs (reduces spinner / idealTree spam in some terminals)
progress=false
audit=false
fund=false

; Reduce ERR_SOCKET_TIMEOUT on slow or flaky links to the npm registry
fetch-timeout=600000
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
